// Count to ten!

// We need to specify...

// Where should we START counting?
// Where is the FINISH line?
// What's the STEP SIZE we should use?

//    START - 1    FINISH @ 10  STEP SIZE + 1
for (let count = 1; count < 11; count += 1) {
  console.log(count);
}

for (let index = 10; index < 101; index += 10) {
  console.log(index);
}
